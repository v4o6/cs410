Wrapper library for libpthreads.
Compile libpthread_wrapper.so by typing 'make'.  To run the test program and generate a log file, type 'make run'.

To test an arbitrary pthreads program and generate a log file, type './run.sh <PROGRAM NAME>' where <PROGRAM NAME> is a path to your program.

